# touring-gacha

バイクツーリング向けランダム目的地提案Webアプリ。Cloudflare Workers + Workers Static Assetsで運用する。

## ドキュメント

- プロジェクト概要・現状ステータス・セットアップ: README.md を参照（初めてこのリポジトリに触れる時はまずここ）
- 詳細要件: docs/requirements.md を参照（全体像を把握したい時に読む。実装作業では都度参照で十分）
- 全体の実装順序・進捗: docs/roadmap.md、機能↔ファイル対応表: docs/feature-map.md
- 画面イメージ: docs/mockups/ 配下のPNGを参照（最初の構想のスナップショット。運用方針は下記「モックアップの扱い」参照）

## モックアップの扱い

- `docs/mockups/` 配下のPNGは**実装が進んでも編集・差し替えをしない**。デザインが大きく変わったりページが増えたりしても、実装後の状態への追従はしない（最初の構想のスナップショットとして固定する）。理由は2つ: (1) gitはバイナリ画像の差分管理が苦手で、更新のたびに履歴が肥大化するため、頻繁な差し替えは避けるべき (2) 現状の正確な仕様は要件定義・decision記録側で追跡すれば十分で、モック側の追従は必須ではない。モックアップと実装が乖離した場合は、その乖離を`docs/decisions/`に明記する（例: docs/decisions/260829-decide-button-removal.md）
- 見た目に関わる変更を確認してもらう際は、テキストの説明だけで済ませず、**必ず実際に開ける画像を用意する**。作業の節目ごとの確認には、`docs/_tmp-preview/`のようなワークスペース内の一時的な場所にスクリーンショットを置き、相対パスのMarkdownリンクで提示する。確認が済んだら削除し、コミットしない
- ロードマップ全体（docs/roadmap.mdの全フェーズ）が完了した時点で一度だけ、実際の画面のスクリーンショット一式を「as-built」の記録として残す（都度ではなく、最後にまとめて）

## 構成

- `src/` : Cloudflare Workers側（API・目的地探索ロジック、TypeScript、Hono）
- `public/` : Static Assets配信対象。バンドラーなしの素のHTML/CSS/JS（ES Modules）。1枚のindex.htmlを画面状態の切り替えで構成する。MapLibre GL JSはCDNからESモジュールとしてimportする（v6以降はUMD/グローバル公開ビルドが廃止されたため、`<script src>`ではなく`import`を使うこと）
- `docs/` : 要件・画面イメージ・設計判断の記録

## 作業の進め方（計画・実行記録）

- 何らかの作業に着手する前に、原則としてまず計画を立て、`docs/plans/` 配下にファイルとして書き出す。ファイル名は `yymmdd-hhmmss-内容.md` の形式とする（日付のみだと同日内の前後関係が分からなくなるため、時刻まで含める）。内容部分は日本語でよい。英語の方が端的な場合は英語でも構わない（例: `docs/plans/260828-153045-計画ルール追加.md`、`docs/plans/260828-160210-fix-highway-buffer.md`）
- 実行結果は別ファイルに起こさず、同じ計画ファイルに追記して記録する（機械的に処理できるため、計画ファイル自体を実行結果の記録先として兼用する）
- 計画通りに実行できた場合は、結果として残しておくべき事柄を計画ファイルに追記する
- 計画から変更して実行した場合は、その変更点を計画ファイル末尾に「変化一覧」という項目としてまとめて追記する
- **要件・仕様に「選択可能な項目の組み合わせ」（設定の掛け合わせ）や「離散的な選択肢を連続範囲に変換する設計」（探索範囲の帯など）を導入する際は、個々の選択肢を単体で見るだけでなく、あらゆる組み合わせ・境界を実際に確認すること。** (1)結果が空・無意味になる組み合わせがないか（例: 選んだコピー項目がその候補にたまたま存在せず、コピー結果が空になる）、(2)選択肢全体でカバーすべき範囲に意図しない抜けがないか（例: 探索範囲の帯が選択肢の間で中抜けする）を検証する。個々の要素は正しくても、組み合わせ・境界でしか気づけない見落としがある（実際に発生・詳細: docs/decisions/260829-copy-preference.md の追記, docs/decisions/260829-search-radius-band-gapless.md）
- **同じ対象（同じデータ）を表示する複数箇所のUIの見た目（スタイル・コンポーネント）を統一・共通化する際は、その中に表示する内容（データ・情報量）も揃っているかを実装後に横並びで確認すること。** 見た目が異なっている間は表示内容の差異が目立たず気づかれにくいが、見た目を揃えるとその差がかえって目立つようになる。見た目の統一だけに気を取られ、中身の一致確認が漏れやすい（実際に発生・詳細: docs/plans/260829-211319-結果画面下部シート再構成.md の追記。結果画面の目的地情報〈折りたたみ時〉と、展開後の周辺情報一覧の目的地行とで、現在地からの距離の有無が食い違っていた）
- **キャッシュを導入・設計する際は、キャッシュキーの粒度が「結果が実際にどの単位で変わるべきか（ランダム性・個別性の源）」と一致しているかを確認すること。** 既存の似たキャッシュキーパターンをそのまま流用すると、生成ロジック自体は正しくても、その中の乱数選択の結果がキャッシュキーの外側（毎回同じキーで共有される範囲）に置かれてしまい、実質的に個別性が消える（毎回似た結果しか出ない）ことがある。外形的には乱数バグに見えるが、原因はキャッシュ設計の側にある。生成過程で乱数・個別の選択を伴う機能にキャッシュを追加する際は特に注意する（実際に発生・詳細: docs/plans/260830-073043-交差点キャッシュを仮地点ベースに変更.md。交差点候補のキャッシュキーを、既存カテゴリと同じ「現在地・探索範囲」ベースのまま流用したため、実際にランダム性の源であるはずの「仮地点」の乱数決定が、同じ現在地・探索範囲での検索・再抽選のあいだ実質共有されてしまい、毎回同じような結果しか出なくなっていた）

## 守るべきドメインルール

- 高速道路・高規格道路は候補から常時除外し、そこから100m以内の候補地点も除外する
  - 実装上の注意: Overpass QLの`around.SET`による2集合間の近接フィルタ（例: `nwr.cand(around.hw:100)`）は計算コストが非常に高く、`shop=*`のような件数の多いカテゴリではタイムアウトする（実測済み）。高速道路除外は「候補検索」と「高速道路ジオメトリ取得（`out geom`）」を別々のOverpass文にして両方取得し、アプリ側（Workers）で点と線分の距離計算により除外する（`src/lib/highway-filter.ts`, `src/lib/geo.ts`の`distanceToPolylineMeters`）
- 探索範囲は指定距離に近い帯にある地点を候補とする。選択肢どうしの間に中抜けができないよう、隣り合う選択肢との中間値を境界にする（両端の選択肢だけ指定距離の10%と固定5kmの大きい方をマージンとする。詳細: docs/decisions/260829-search-radius-band.md, docs/decisions/260829-search-radius-band-gapless.md）。帯の範囲内に候補がなければ範囲を自動的に広げず、見つからなかった旨を伝える
- 候補地点は交差点など地点として区別できる場所に限定し、直線路途中の道端・行き止まりは除外する
- 目的地種別は「スポット（店）」「スポット（店以外）」「交差点」の3種類を切り替えられるようにする
  - スポット（店）: 食事・休憩／買い物・その他
  - スポット（店以外）: 停車場所の要否を選択（地図データで判断可能な場合のみ条件化）
  - 交差点: 詳細条件は設けない。道路網の次数（ノードを共有する異なるway数）が3以上の地点を候補とする。仮地点（現在地から乱数の方角×指定距離ちょうど）周辺のみを探索するため、候補が仮地点付近に局所的に集まる。この性質が「スポット（店以外）」（帯全体から均等に出る）と異なるため、候補プールを混ぜずに種別を分けている（詳細: docs/decisions/260830-phase4b交差点検出実装.md）
- ナビ連携は情報のコピーで十分。ナビアプリへの直接連携は不要
  - コピー項目（緯度経度／名称／住所）の選択はコピーの都度ではなくアプリ全体で1回設定し、localStorageに保存する。目的地・周辺POIどちらのコピーもこの設定に従う（詳細: docs/decisions/260829-copy-preference.md）
- 周辺情報一覧は画面遷移せず、地図確認の流れの中で1操作で表示できるようにする。周辺POIも目的地と同様にコピー対象とする

## 運用・インフラ方針

- Cloudflare Workers（Hono） + Workers Static Assets（バンドラーなし） + Workers KV（すべて無料枠）で運用する
- 無料枠は未来永劫でなくてよい。自分ひとりが使う規模で維持できれば十分とする
- 無料枠を超えた場合は自動課金ではなく、サービス利用不可（停止）になることを許容する
- Overpass APIのレート制限を吸収するため、KV導入は「要否を検討する」段階を経ず、探索結果のキャッシュ用途として前提にする
- 地図ライブラリはMapLibre GL JS、タイル提供元はMapTiler無料プラン（月10万マップロードで自動課金なく停止）を採用する
- 詳細な経緯は docs/decisions/260828-infra-cloudflare-kv.md を参照
- **KVキャッシュのキーに含めるバージョン番号（`ov:v1`, `poi:v1`等）は、キャッシュに保存する値の中身・意味を決めるロジックを変更するたびに必ず上げる。** データ型が同じでも、フィルタ条件などで「どの候補が生き残ってキャッシュされるか」が変われば対象。上げ忘れると、ロジック変更前の古いキャッシュがTTL満了まで（最大24時間）そのまま使われ続け、修正が反映されない不具合になる（実際に発生・詳細: docs/decisions/260829-cache-key-versioning.md）

## 現状の未確定事項

このファイルには極力書かない（更新が漏れて陳腐化しやすいため）。最新の未確定事項・進捗はdocs/roadmap.mdを参照。OSMデータ取得方法・D1導入要否など、以前ここに列挙していた個別項目はすべて確定済み（docs/roadmap.md参照）。

## コマンド

- `npm install` : 依存パッケージインストール
- `npm run dev` : ローカル開発サーバー起動（`wrangler dev`）
- `npm run typecheck` : 型チェック（`tsc --noEmit`）
- `npm run deploy` : Cloudflareへデプロイ（`wrangler deploy`）
- 初回のみ `npx wrangler login` でCloudflareアカウントの認証が必要
